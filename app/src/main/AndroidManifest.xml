<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizApplication"
        tools:targetApi="31">



        <activity android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Login.LoginActivity" />
        <activity android:name=".activities.Login.SignupActivity" />
        <activity android:name=".activities.admin.AdminActivity" />
        <activity android:name=".activities.admin.AdminManagementActivityQuizzes" />
        <activity android:name=".activities.admin.AdminManageActivityCategories" />
        <activity android:name=".activities.admin.AdminManagementActivityAnswers" />
        <activity android:name=".activities.admin.AdminAddAnswerActivity" />

        <activity android:name=".activities.user.UserActivity" />
        <activity android:name=".activities.user.WaitingActivity" />
        <activity android:name=".activities.user.QuizActivity" />
        <activity android:name=".activities.user.CongratulationActivity" />
    </application>
</manifest>